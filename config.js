/* ============================================================
   OcePvP - suggestion settings
   ------------------------------------------------------------
   Suggestions from the Games page get posted straight into a
   Discord channel using a webhook.

   HOW TO GET THE URL:
     1. In Discord, right-click the channel you want them in
        (a staff-only one) -> Edit Channel
     2. Integrations -> Webhooks -> New Webhook
     3. Name it something like "OcePvP Suggestions"
     4. Click "Copy Webhook URL"
     5. Paste it between the quotes below, save, and push.

   Left empty, the form tells people to use the Discord instead.

   ⚠️ Anyone who views this page's source can read this URL. With
   it they can post messages to that channel or delete the
   webhook. If that ever happens: delete the webhook in Discord,
   make a new one, and paste the new URL here. Point it at a
   staff channel, never anything important.
   ============================================================ */

const OCE_WEBHOOK = "";

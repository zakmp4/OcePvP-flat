/* ============================================================
   OcePvP - API address
   ------------------------------------------------------------
   Paste your Render Web Service URL between the quotes, with no
   slash on the end. This is the ONLY line you change to connect
   the suggestion form and the admin panel to the database.

       const OCE_API = "https://ocepvp-api.onrender.com";

   Left empty, the suggestion form tells people it isn't set up
   yet, and the admin panel says the same.
   ============================================================ */

const OCE_API = "";
